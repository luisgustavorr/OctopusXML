class localPrinter {
    text(text, UTF) {
        console.log(text);
        return this; // Retorna o próprio objeto para encadeamento de métodos
    }

    align() {
        return this; // Retorna o próprio objeto para encadeamento de métodos
    }

    style() {
        return this; // Retorna o próprio objeto para encadeamento de métodos
    }

    font() {
        return this; // Retorna o próprio objeto para encadeamento de métodos
    }
    cut() {
        return this; // Retorna o próprio objeto para encadeamento de métodos
    }
    close() {
        return this; // Retorna o próprio objeto para encadeamento de métodos
    }
    size(one, two) {
        return this; // Retorna o próprio objeto para encadeamento de métodos
    }
}

module.exports = localPrinter;
